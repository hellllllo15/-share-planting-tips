spring:
  application:
    name: share-planting-tips
server:
  port: 8123
  servlet:
    context-path: /api

# springdoc-openapi
springdoc:
  group-configs:
    - group: 'default'
      packages-to-scan: com.example.code.controller

knife4j:
  enable: true
  setting:
    language: zh_cn
